/**
 * Global namespace
 */
var APP = APP || {};


(function () {
	// Init table
	APP.table.init('.js-table');
	// Init employee
	APP.employee.init('.js-employee-modal');
	// Init shift
	APP.shift.init('.js-shift-modal');
}());